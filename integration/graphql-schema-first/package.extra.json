{
  "scripts": {
    "prepare:integration": "zelt graphql codegen --schema src/graphql/schema.graphql --out src/generated/graphql.ts && zelt graphql codegen --schema src/graphql/admin.graphql --out src/generated/admin.ts && vitest run e2e/generated.spec.ts"
  }
}
