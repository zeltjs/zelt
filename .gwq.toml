[[repository_settings]]
repository = '.'
copy_files = ['lefthook-local.yml']
setup_commands = [
  'ROOT=$(git worktree list | head -1 | awk "{print \\$1}") && mkdir -p .codepolicy && cp -r "$ROOT/.codepolicy/cache" .codepolicy/ 2>/dev/null || true',
  'pnpm i',
]
